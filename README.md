# Softify

Marketing site for Softify — IT solutions for radio broadcasting.

Static site (HTML + React via Babel standalone + CSS). No build step.

## Local preview

```sh
python3 -m http.server 8000
```

Then open <http://localhost:8000>.

## Hosting

Deployed via GitHub Pages from the `main` branch.
